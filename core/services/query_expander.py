class QueryExpander:

    def expand(self, query: str):

        base = query.lower()

        return [
            base,
            base + " structural consultancy india",
            base + " rfp pdf",
            base + " eprocurement",
            base + " delhi ncr",
            base + " up haryana",
        ]